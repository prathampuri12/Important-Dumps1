how to pull requests

fork the directory first
clone or download option mein copy the link and click on the button side mein and rename it
then in git bash
cd name of the one which you forked//to work in the repository
notepad README.md(like to have changes in it done by you or if in jupyter then write jupyter filename.ipynb and do save it)
git status(to see if we made any changes to any thing or not)
git add filename.ipynb(if python file)
git commit -m "anything"
git push -u origin master
and then have a pull request to the one which you forked that